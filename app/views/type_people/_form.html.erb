<%= form_with(model: type_person) do |form| %>
  <% if type_person.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(type_person.errors.count, "error") %> prohibited this type_person from being saved:</h2>

      <ul>
        <% type_person.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :slug, style: "display: block" %>
    <%= form.text_field :slug %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
